/**
 * metadata.ts — Tags & custom metadata JXA templates
 *
 * Note: Write operations (addTags, setCustomMetadata) have been intentionally
 * removed to enforce the strict read-only principle of this Agent.
 * Metadata is only read via getRecordMetadataScript in records.ts.
 */
